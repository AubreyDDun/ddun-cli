import { commonRequest, CodeError } from '@/utils/request'

/**
 * example
 */
// import {
//   CoreIndicatorTreeNode,
// } from '@/views/CoreIndicators/types'

// // 获取核心指标树
// const getIndicatorTree = async (): Promise<CoreIndicatorTreeNode[] | void> => {
//   const res = await commonRequest.get('/apm-admin/indicatorClassify/getIndicatorTree')
//   if (res !== undefined && res.code === 0) return res.result
//   throw new CodeError(res.code, res.message)
// }

export default {
  // getIndicatorTree,
}
